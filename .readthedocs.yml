version: 2

build:
  apt_packages:
  - graphviz
  - tree
  os: ubuntu-24.04
  tools:
    python: '3.12'

python:
  install:
  - method: uv
    command: sync
    path: aiida-core
    extras:
    - docs
    - tutorials
    - tests
    - rest
    - atomic_tools

sphinx:
  builder: html
  fail_on_warning: true
  configuration: aiida-core/docs/source/conf.py

search:
  ranking:
    reference/apidoc/*: -7
