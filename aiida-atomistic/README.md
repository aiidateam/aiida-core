# aiida-atomistic

Atomistic (materials-science) data types and tools for [AiiDA](http://www.aiida.net/), extracted from `aiida-core`.

Requires `aiida-core` and the former `aiida-core[atomic_tools]` dependencies (ASE, pymatgen, spglib, seekpath, PyCifRW, ...).
