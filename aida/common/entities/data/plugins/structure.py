"""
This plugin is to manage generic structures similar to an ase.Atoms class.
Most of the implementation is already ready in the repository (Site/Sites classes).
See there for the methods (as .to_ase(), .add_site(), .is_alloy(), ...)
"""
