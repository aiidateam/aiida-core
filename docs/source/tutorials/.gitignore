# Artifacts generated by Module 0's `gsrd` runs (which use the current
# directory as scratch); kept out of the source tree.
/results.npz
/input.yaml

# Isolated AiiDA config dir created by the tutorial setup cell.
/.aiida-tutorial/
