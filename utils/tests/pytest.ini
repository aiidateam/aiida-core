[pytest]
# Standalone config so the utils tests do not inherit the root pyproject.toml
# pytest configuration (which references aiida-core dependencies such as
# sqlalchemy in its warning filters). These tests are pure standard library.
