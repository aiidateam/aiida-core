.. warning::

   This is work in progress and only available on the development branch

Scripting API
=============

The `aiida.control` module contains user facing APIs to control various aspects of AiiDA from scripts, interactive prompts, etc.

The `verdi` command provides a command line interface to it's API.

.. toctree::

   postgres
