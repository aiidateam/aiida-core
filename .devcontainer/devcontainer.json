{
    "build": {
        // Path is relataive to the devcontainer.json file.
        "dockerfile": "../Dockerfile"
    },
    "postCreateCommand": [
        "pip install ./aiida-core[tests,pre-commit]",
        "/opt/configure-aiida.sh"
    ],
    "waitFor": "postCreateCommand"
}